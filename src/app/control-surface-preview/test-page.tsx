/**
 * Simple test page to verify routing works
 */

export default function TestPage() {
  return (
    <div style={{ padding: '40px', backgroundColor: '#0a0a0a', color: '#e5e5e5', minHeight: '100vh' }}>
      <h1 style={{ fontSize: '32px', marginBottom: '20px' }}>Test Page Works!</h1>
      <p>If you can see this, routing is working.</p>
      <p>Now check: /control-surface-preview</p>
    </div>
  );
}

