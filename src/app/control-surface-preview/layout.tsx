/**
 * Control Surface Preview Layout
 * 
 * This layout imports the control surface CSS for preview purposes.
 */

import '../globals-control-surface.css';

export default function ControlSurfacePreviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

